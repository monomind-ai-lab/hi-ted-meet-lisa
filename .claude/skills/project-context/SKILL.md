---
name: project-context
description: "Use when this repository's current state, prior decisions, verified learnings, or linked evidence need to be read or maintained — before meaningful work, when resuming, and when handing off. The protocol lives in .agents/skills/project-context/SKILL.md."
allowed-tools: Read, Glob, Grep, Bash, Edit, Write
---

# Project Context

This repository keeps its skills harness-neutral under `.agents/skills/`.

Read `.agents/skills/project-context/SKILL.md` and follow it exactly. It is the
protocol; this file only makes it reachable as `/project-context` in Claude Code
and discoverable by description when no hook has run.

`project-context/SKILL.md` is this repository's own instance of the protocol.
Read it too when the two differ — it records local profile and wiring.
